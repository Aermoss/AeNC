var destroyed = false

while (not var destroyed) {
    var command = input ("> ")

    id (var command == "EXIT") {
        print ("Exiting...")
        var destroyed = true
    }

    if (var command != "EXIT") {
        system (var command)
    }
}
